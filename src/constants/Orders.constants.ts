export enum SIDE {
    BUY = 'buy',
    SELL = 'sell',
}

export enum ORDER_TYPE {
    LIMIT = 'limit',
    MARKET = 'market',
}

export enum CATEGORY {
    LINEAR = 'linear',
    INVERSE = 'inverse',
}
